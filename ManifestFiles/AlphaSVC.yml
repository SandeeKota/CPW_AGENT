apiVersion: v1
kind: Service
metadata:
  name: "alpha-dashboard"
  namespace: cpw-alpha
spec:
  selector:
    app: "dashboard"
  type: NodePort
  ports:
    - port: 4444
      protocol: TCP
      targetPort: 3000
